# ~/.bashrc: executed by bash(1) for non-login shells.

{{ if (eq .chezmoi.chassis "ephemeral") -}}

export SHELL=$(which zsh)
if [[ -o login ]]
then
#    exec zsh -l
else
#    exec zsh
fi

{{ end -}}
