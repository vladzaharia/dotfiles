# Generated by chezmoi — do not edit directly
# Regenerate: chezmoi apply

{{- range $group, $data := .packages }}
{{-   if or (index $data "required") (has $group $.package_groups) }}
{{-     $darwin := index $data "darwin" | default dict }}
{{-     range (index $darwin "tap" | default list) }}
tap "{{ . }}"
{{-     end }}
{{-   end }}
{{- end }}

{{- range $group, $data := .packages }}
{{-   if or (index $data "required") (has $group $.package_groups) }}
{{-     $darwin := index $data "darwin" | default dict }}
{{-     range (index $darwin "brew" | default list) }}
brew "{{ . }}"
{{-     end }}
{{-     range (index $darwin "cask" | default list) }}
cask "{{ . }}"
{{-     end }}
{{-     range (index $darwin "mas" | default list) }}
mas "{{ . }}"
{{-     end }}
{{-   end }}
{{- end }}
