# Generated by chezmoi — do not edit directly
# Regenerate: chezmoi apply

{{- range $group, $data := .packages }}
{{-   if or $data.required (has $group $.package_groups) }}
{{-     range (index $data "darwin" | default dict | index "tap" | default list) }}
tap "{{ . }}"
{{-     end }}
{{-   end }}
{{- end }}

{{- range $group, $data := .packages }}
{{-   if or $data.required (has $group $.package_groups) }}
{{-     range (index $data "darwin" | default dict | index "brew" | default list) }}
brew "{{ . }}"
{{-     end }}
{{-     range (index $data "darwin" | default dict | index "cask" | default list) }}
cask "{{ . }}"
{{-     end }}
{{-     range (index $data "darwin" | default dict | index "mas" | default list) }}
mas "{{ . }}"
{{-     end }}
{{-   end }}
{{- end }}
