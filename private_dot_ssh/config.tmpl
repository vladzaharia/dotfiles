{{ if not .flags.ephemeral -}}

Include {{ .chezmoi.homeDir }}/.colima/ssh_config

{{-   if .flags.pokemon }}
Include {{ .chezmoi.homeDir }}/.ssh/pkmn.config
{{-   end }}

Host router
    hostname router.zhr.one
    user root
    port 22

Host truenas
    hostname 10.10.0.10
    user vlad
    port 22

# Docker via Tailscale
Host docker-ts
    hostname docker
    port 22

# Docker
Host docker
    hostname 10.10.1.20
    port 22
{{ end -}}
