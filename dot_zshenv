function vssh() {
    mode=ca
    role=user
    username=$USER
    host=$1

    if [[ $1 == -r* ]]; then
        role=root
        username=root
        host=$2
    fi
    vault ssh -mode=$mode -role=$role $username@$host
}
